# GitHub Actions run 31035868066

- Workflow: CI
- Head SHA: `90384499bced4f8d006260195bd9ce090ed0ff90`
- Conclusion: failure
- Ruff and formatting: passed
- Failure step: pytest collection
- Root cause: the Linux runner could not import `scripts.*` because the repository lacked `scripts/__init__.py`.
- Repair: add the package initializer and rerun the exact workflow.

The follow-up run showed that the console-script pytest entry point also needs the repository root on `sys.path`. The workflow repair therefore sets `PYTHONPATH` to `${{ github.workspace }}` for the CI job.

The subsequent run 31037460885 then reached all tests and exposed the next environment issue: benchmark schema tests assumed large local PPO/SAC checkpoint files. The benchmark manifest now uses a deterministic sentinel hash for absent local-only checkpoints during manifest construction; actual benchmark execution still records unresolved load failures rather than claiming a run passed.
